import React from 'react';

export const PageNotFound = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <div style={{ fontSize: 60, fontWeight: 'bold' }}>Page not found!</div>
    </div>
  );
};
